//this is form compo

export default function Form(){
    // we need state to save the formData
    // ?formData = {
    //  ?   key:value,
    //  ?   key:value
    // ?}
    
    
    //! dont need useEffecthere , mybe somethin else
    
    // a handlesubmitfunc
    // fucntion handleSubmit (){
    //     //? somethign to handle default behaviour of form
    //     //? also somethign that fetches the POST end point
    // fetch ("url", {
    //     method:
    //     body:
    //     headers:{"json/applocation":"content/type"}
    // })
    
    
    //? a function to handle change of uUser input
    
    function handleChange(){
        //we need to add the values form the initial state 
        //nned to sset the properties for the new bject.
        // they key is the taregrt name, value is the target value
    }
    }
    
    
    
        return<> 
        {/* //need to have form here with 2 events, on to submit and one to track changes
    //! remember to be consistent with how you name the name attirbute. the name attirbute in the inut should be the same as the database column where you are storing the data */}
        FORM</>
    }